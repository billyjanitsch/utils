{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "fix": "oxfmt && oxlint --fix",
    "test": "oxfmt --check && oxlint && pnpm -r test"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.32.1"
}
