{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "fix": "oxfmt && oxlint --fix",
    "test": "oxfmt --check && oxlint && pnpm -r test",
    "release": "pnpm -r build && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.32.1"
}
