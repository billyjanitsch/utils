{
  "private": true,
  "scripts": {
    "fix": "oxfmt && oxlint --fix",
    "test": "oxfmt --check && oxlint && pnpm -r test"
  },
  "devDependencies": {
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0"
  }
}
